<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Member">

	<select id="getMember_login" resultType="member">
		select userid, username from member
		where userid=#{userid} and password=#{password}
	</select>
	
	<insert id="insertMember">
		insert into member (userid, username, password, email)
		value
		(#{userid}, #{username}, #{password}, #{email})
	</insert>
	<update id="updateMember">
		update member
		set userid=#{userid}, username=#{username}, password=#{password}, email=#{email}
		where sid=#{sid}
	</update>
	<delete id="deleteMember">
		delete from member where sid=#{sid}
	</delete>
	<select id="getMember" resultType="member">
		select * from member where sid=#{sid}
	</select>
	<select id="getMemberList" resultType="member">
		select * from member order by sid desc
	</select>
	
</mapper>
